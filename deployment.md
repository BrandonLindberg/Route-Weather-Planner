## Development Setup

Run "npm install" to install required packages.
Create .env file and use ".env.example" to get correct api key format and required keys.

To start backend, run "npm run dev:server".
To start frontend, run "npm run dev".
You will need two terminals open to run this correctly, one command in each terminal.

Once running, frontend is hosted on "http://localhost:5173"